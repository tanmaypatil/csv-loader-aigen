# Test Database Configuration for PostgreSQL
# Update these values to match your test PostgreSQL instance
db.url=jdbc:postgresql://localhost:5432/employee
db.username=apple
db.password=mac
db.driver=org.postgresql.Driver

# Date format configuration
date.format=yyyy-MM-dd

# CSV configuration
csv.delimiter=,
csv.has.header=true
