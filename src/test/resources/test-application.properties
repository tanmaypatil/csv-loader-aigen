# Test Application Configuration

# DateTime format configuration (ISO 8601 with milliseconds)
date.format=yyyy-MM-dd'T'HH:mm:ss.SSSXXX

# CSV configuration
csv.delimiter=,
csv.has.header=true

# CSV files to import for testing
csv.files=employee.csv
